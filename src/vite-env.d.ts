/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/vanillajs" />

// Injected by vite.config.mts's `define` -- the commit sha (or timestamp for
// local builds) this bundle was built from. See useAppUpdateCheck.ts.
declare const __BUILD_ID__: string;
